{
  "name": "membership",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ swissdao/membership",
    "create-local": "graph create --node http://localhost:8020/ swissdao/membership",
    "remove-local": "graph remove --node http://localhost:8020/ swissdao/membership",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 swissdao/membership",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
